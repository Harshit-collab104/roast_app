name: Test Action

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 24f3001108@ds.study.iitm.ac.in
        run: echo "Workflow running"

      - name: Checkout
        uses: actions/checkout@v4
